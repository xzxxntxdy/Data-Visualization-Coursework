// ============================================================================
// COCO-Verse 项目演示文稿
// 编译方式：typst compile main.typ
// 需要 Touying 包：@preview/touying:0.6.1
// ============================================================================

#import "@preview/touying:0.6.1": *
#import themes.metropolis: *

#show: metropolis-theme.with(
  aspect-ratio: "16-9",
  config-info(
    title: [可视化大作业],
    subtitle: [数据可视化课程项目],
    author: [匡航逸 · 柯云超 · 谢博],
  ),
)

#set text(
  font: (
    "Noto Sans CJK SC",
    "Fira Code",
  ),
)

// = 作为文档标题，== 开始真正的 slide


// ============================================================================
// 封面页
// ============================================================================
== COCO-Verse：多视图可视化分析系统

#align(center)[
  #text(size: 1.2em)[数据可视化课程大作业]
  
  #v(0.5em)
  组名：你说的队
  #v(0.5em)
  组员：匡航逸 , 柯云超 , 谢博
  
  #v(1em)
  
]

// ============================================================================
// 数据介绍
// ============================================================================
== 数据介绍

#grid(
  columns: (1fr, 1fr),
  column-gutter: 2em,
  [
    === COCO 2017 数据集
    
    *Common Objects in Context* 是计算机视觉领域最具影响力的大规模数据集：
    
    - *图像规模*：123,287 张训练/验证图像
    - *物体类别*：80 个常见物体类别
    - *实例标注*：860,000+ 边界框与分割标注
    - *姿态标注*：250,000+ 人体关键点标注
    
    #v(0.5em)
    
    *数据来源*：#link("https://cocodataset.org/")[cocodataset.org]
  ],
  [
    === 类别涵盖
    
    #table(
      columns: (auto, auto),
      stroke: 0.5pt + gray,
      [超类别], [示例],
      [人], [person],
      [交通工具], [car, bicycle, airplane],
      [动物], [dog, cat, bird, horse],
      [室内物品], [chair, couch, bed],
      [厨房用品], [bottle, cup, fork, knife],
      [电子设备], [laptop, cell phone, tv],
      [运动器材], [sports ball, tennis racket],
      [食物], [pizza, cake, banana],
    )
  ]
)

// ============================================================================
// 分析目标
// ============================================================================
== 分析目标

#grid(
  columns: (1fr, 1fr, 1fr),
  column-gutter: 1.5em,
  [
    #rect(
      width: 100%,
      fill: rgb("#e0f2fe"),
      radius: 8pt,
      inset: 1em,
    )[
      === 空间分布分析
      
      - 不同类别的物体在图像中如何分布？
      - 边缘 vs 中心位置偏好？
      - 小/中/大目标的空间特征？
      
      #v(0.5em)
      #text(fill: gray, size: 0.9em)[
        揭示视觉构图规律
      ]
    ]
  ],
  [
    #rect(
      width: 100%,
      fill: rgb("#dcfce7"),
      radius: 8pt,
      inset: 1em,
    )[
      === 语义共现分析
      
      - 哪些物体经常同时出现？
      - 条件概率关系如何？
      - 是否存在语义场景聚类？
      
      #v(0.5em)
      #text(fill: gray, size: 0.9em)[
        挖掘物体关联模式
      ]
    ]
  ],
  [
    #rect(
      width: 100%,
      fill: rgb("#fef3c7"),
      radius: 8pt,
      inset: 1em,
    )[
      === 人体姿态分析
      
      - 关键点可见性分布如何？
      - 不同场景下的典型姿态？
      - 遮挡模式有何规律？
      
      #v(0.5em)
      #text(fill: gray, size: 0.9em)[
        理解人体行为特征
      ]
    ]
  ]
)

// ============================================================================
// 数据处理流程
// ============================================================================
== 数据处理流程

#align(center + horizon)[
  #grid(
    // 关键点1：使用 1fr 强制三列等宽，限制盒子的最大宽度
    columns: (1fr, auto, 1fr, auto, 1fr), 
    column-gutter: 0.5em,
    row-gutter: 1em,
    
    // --- 第 1 列 ---
    [
      #rect(
        width: 100%, // 关键点2：盒子填满列宽
        fill: rgb("#f0f9ff"),
        radius: 8pt,
        inset: 0.8em,
      )[
        // 标题保持原有大小
        #text(weight: "bold")[原始数据]
        
        #v(0.5em)
        #set text(size: 0.7em) // 关键点3：全局缩小列表文字
        #set par(leading: 0.4em) // 缩小行间距
        
        - COCO 图像集
        - instances_train2017.json
        // 单独处理超长文件名，强制换行
        - person_keypoints\_ \ train2017.json
        
        #v(0.5em)
        #text(size: 0.9em, fill: gray)[~19GB]
      ]
    ],
    
    // --- 箭头 ---
    [#align(center + horizon)[→]],
    
    // --- 第 2 列 ---
    [
      #rect(
        width: 100%,
        fill: rgb("#fefce8"),
        radius: 8pt,
        inset: 0.8em,
      )[
        #text(weight: "bold")[Python 预处理]
        
        #v(0.5em)
        #set text(size: 0.7em) 
        #set par(leading: 0.4em)
        
        - find_image.py
        - save_overview.py
        - process_spatial.py
        - process_semantic.py
        - process_pose.py
        
        #v(0.5em)
        #text(size: 0.9em, fill: gray)[采样 + 聚合]
      ]
    ],
    
    // --- 箭头 ---
    [#align(center + horizon)[→]],
    
    // --- 第 3 列 ---
    [
      #rect(
        width: 100%,
        fill: rgb("#f0fdf4"),
        radius: 8pt,
        inset: 0.8em,
      )[
        #text(weight: "bold")[前端资源]
        
        #v(0.5em)
        #set text(size: 0.7em)
        #set par(leading: 0.4em)
        
        - hero_image.jpg
        - overview.jpg
        - hero_data.json
        - spatial_data.json
        - semantic_data.json
        - pose_stats.json
        
        #v(0.5em)
        #text(size: 0.9em, fill: gray)[~3MB]
      ]
    ],
  )
]

#v(1em)

#text(size: 0.9em)[
  *数据转换要点*：
  - *门户资源*：筛选最佳 Hero Image 并生成概览拼图
  - *空间采样*：从 860K 标注中采样 8,000 条
  - *共现矩阵*：计算 80×80 条件概率
  - *姿态聚合*：统计 17 个关键点的可见/不可见/标注概率
]


// ============================================================================
// 系统架构
// ============================================================================
== 系统架构

#columns(2, gutter: 1em)[
  #image("images/portal_demo.png", width: 100%)
  #image("images/portal_demo1.png", width: 100%)
  #image("images/portal_demo2.png", width: 100%)
  #image("images/portal_demo3.png", width: 100%)
]

// ============================================================================
// 可视设计总览
// ============================================================================
== 可视设计（总览）

#grid(
  columns: (1fr, 1fr, 1fr),
  column-gutter: 0.2em,
  [
    #rect(
      fill: rgb("#dbeafe"),
      radius: 8pt,
      inset: 1em,
    )[
      === 📍 空间与尺度视图
      
      - 等高线密度热力图
      - 位置×尺度散点图
      - 类别尺度分布图
      
      #text(size: 0.8em, fill: gray)[
        *目的*：揭示数据偏见，指导数据增强策略。
      ]
    ]
  ],
  [
    #rect(
      fill: rgb("#d1fae5"),
      radius: 8pt,
      inset: 1em,
    )[
      === 🕸️ 语义共现网络
      
      - 力导向节点-链接图
      - 条件概率侧边栏
      - 共现阈值滑块
      
      #text(size: 0.8em, fill: gray)[
        *目的*：挖掘物体上下文关系，提升模型场景理解能力。
      ]
    ]
  ],
  [
    #rect(
      fill: rgb("#fef9c3"),
      radius: 8pt,
      inset: 1em,
    )[
      === 🦴 人体姿态视图
      
      - 平均姿态与不确定性
      - 关键点可见性雷达图
      - 统计概览与辅助分析
      
      #text(size: 0.8em, fill: gray)[
        *目的*：理解姿态分布规律，聚焦算法挑战（如遮挡）。
      ]
    ]
  ]
)

#v(1em)

#align(center)[
  #image("images/architecture.png", width: 80%)
]

// ============================================================================
// 视图 A：空间与尺度
// ============================================================================
== 视图 A：空间与尺度视图

#grid(
  columns: (1fr, 1fr),
  column-gutter: 1em,
  [
    === 可视映射
    
    #table(
      columns: (auto, auto),
      stroke: 0.5pt + gray,
      [视觉通道], [数据属性],
      [等高线颜色深度], [物体中心点密度],
      [散点 X 坐标], [归一化水平位置 (0~1)],
      [散点 Y 坐标], [相对面积 (对数刻度)],
      [散点颜色], [尺度类别 (小/中/大)],
      [条形长度], [各尺度类别占比],
    )
    
    #v(0.5em)
    
    === 设计决策
    
    - *等高线*：清晰展示密度梯度。
    - *对数刻度*：缓解尺度极值差异。
    - *多视图联动*：结合散点图和分布图。
  ],
  [
    #image("images/spatial_view.png", width: 100%)
  ]
)

// ============================================================================
// 视图 B：语义共现网络
// ============================================================================
== 视图 B：语义共现网络

#grid(
  columns: (1fr, 1fr),
  column-gutter: 1em,
  [
    === 可视映射
    
    #table(
      columns: (auto, auto),
      stroke: 0.5pt + gray,
      [视觉通道], [数据属性],
      [节点大小], [类别出现频次],
      [节点颜色], [超类别 (supercategory)],
      [边粗细], [共现次数],
      [边透明度], [共现强度],
    )
    
    #v(0.5em)
    
    === 设计决策
    
    - *力导向布局*：自然呈现语义聚类。
    - *条件概率 P(B|A)*：揭示非对称关联。
    - *交互式过滤*：通过阈值滑块和双击操作，帮助用户减少视觉杂乱。
  ],
  [
    #image("images/semantic_view.png", width: 100%)
  ]
)

// ============================================================================
// 视图 C：人体姿态
// ============================================================================
== 视图 C：人体姿态视图

#grid(
  columns: (1fr, 1fr),
  column-gutter: 1em,
  [
    === 可视映射
    
    #table(
      columns: (auto, auto),
      stroke: 0.5pt + gray,
      [视觉通道], [数据属性],
      [骨架位置], [平均姿态 (mean_pose)],
      [椭圆区域], [位置不确定性 (std_dev)],
      [雷达图半径], [可见性概率 (visibility)],
      [节点颜色], [身体部位分组],
    )
    
    #v(0.5em)
    
    === 设计决策与目的
    
    - *平均姿态骨架*：展示平均姿态骨架。
    - *不确定性椭圆*：可视化标准差，直观展示关节点的变化范围
    - *可见性雷达图*：清晰对比所有关节点的被遮挡情况，为处理遮挡问题的算法研究提供数据洞察。
  ],
  [
    #image("images/pose_view.png", width: 100%)
  ]
)

// ============================================================================
// 交互设计
// ============================================================================
== 交互设计

=== Cross-Filtering 联动机制

#grid(
  columns: (1fr, 1fr),
  column-gutter: 2em,
  [
    *空间视图交互流程*：
    
    + 在热力图上 *框选* 感兴趣区域
    + 散点图 *高亮* 落入该区域的点
    + 类别分布图 *更新* 为选中区域的统计
    + 点击分布图某一类 → 反向联动热力图
    
    #v(0.5em)
    
    *语义视图交互流程*：
    
    + *单击* 节点：锁定显示条件概率
    + *双击* 节点：从图中排除该类别
    + *拖拽* 节点：调整布局位置
    + *滑块* 筛选：过滤低共现边
  ],
  [
    #image("images/spatial_view.png", width: 100%)
  ]
)

// ============================================================================
// 案例分析 1
// ============================================================================
== 案例分析 1：交通工具的空间分布规律

#set text(size: 0.95em)
#grid(
  columns: (1fr, 1fr),
  column-gutter: 2em,
  [
    === 分析过程
    
    + 在空间视图的类别筛选器中选择 `car`。
    + *观察热力图*：高密度区域明显集中在图像的 *下半部分*。
    + *查看散点图*：大多数 `car` 实例的相对面积（Y轴）位于中下区域，表明多为 *中小尺度* 目标。
    + 切换类别至 `umbrella` 进行对比，发现其高密度区域集中在 *图像上半部分*。
    
    #v(0.5em)
    
    === 发现与意义
    
    - *数据偏见*：物体的空间分布遵循强烈的视觉先验（车在地上，雨伞在上方）。这种偏见如果被模型学到，可能会在非常规场景（如放在地上的雨伞）中导致检测失败。
    - *应用价值*：此洞察可指导数据增强，例如通过随机垂直翻转或不常见的裁剪来打破这种空间依赖，提升模型的泛化能力。
  ],
  [
    #image("images/car_spatial.png", width: 100%)
    #image("images/umbrella_spatial.png", width: 100%)
  ]
)

// ============================================================================
// 案例分析 2
// ============================================================================
== 案例分析 2：餐桌场景的语义关联

#set text(size: 0.95em)
#grid(
  columns: (1fr, 1fr),
  column-gutter: 2em,
  [
    === 分析过程
    
    + 在语义视图中单击节点 `dining table` 将其锁定为焦点。
    + *观察条件概率面板*：系统自动计算并显示了当 `dining table` 出现时，其他物体出现的概率。
      - P(chair | dining table) = 0.43
      - P(cup | dining table) = 0.40
    + *观察力导向图*：与 `dining table` 强相关的节点（如 `chair`, `cup`）在布局上自然地向其聚拢。
    
    #v(0.5em)
    
    === 发现与意义
    
    - *上下文知识挖掘*：可视化结果量化了“餐桌”这一核心场景的上下文信息。“椅子”是其最强伴随物，而各种餐具则构成次级关联。
    - *模型能力提升*：这种上下文知识对模型至关重要。例如，在拥挤的场景中，即使“椅子”被部分遮挡，模型也可以利用其与“餐桌”的强关联来提高检测的置信度。
  ],
  [
    #image("images/dining_semanic.png", width: 100%)
    #image("images/prob_view.png", width: 100%)
  ]
)

// ============================================================================
// 案例分析 3
// ============================================================================
== 案例分析 3：人体姿态的稳定性与遮挡模式

#set text(size: 0.95em)
#grid(
  columns: (1fr, 1fr),
  column-gutter: 2em,
  [
    === 分析过程
    
    + *观察可见性雷达图*：发现 `肩膀` 区域半径最长 (可见性约 51%)，而 `脚踝` 区域半径最短 (可见性约 28%)。
    + *观察平均姿态骨架*：在左侧视图中，将鼠标悬停于不同关节点，对比弹出的 *不确定性区域*（椭圆）。
    + *对比椭圆大小*：`眼睛`、`鼻子` 的椭圆区域最小；而 `手腕`、`手肘` 的椭圆区域则显著更大。
    
    #v(0.5em)
    
    === 发现与意义
    
    - *遮挡模式洞察*：与直觉不同，数据集中最常被完整标注的并非头部，而是`肩膀`。`脚踝`的低可见性说明样本中存在大量半身像或脚部被遮挡的情况。这为姿态估计算法提供了明确的挑战重点。
    - *关节点稳定性量化*：`眼睛`和`鼻子`最稳定，是姿态中的“锚点”；而`手腕`和`手肘`最“灵活”，活动范围最广。该发现量化了不同身体部位的运动学约束，可用于指导更优的姿态识别模型设计。
  ],
  [
    #image("images/pose_all.png", width: 100%)
    #image("images/pose_intro.png", width: 100%)
  ]
)

// ============================================================================
// 总结
// ============================================================================
== 总结与展望

#grid(
  columns: (1fr, 1fr),
  column-gutter: 2em,
  [
    === 项目贡献
    
    - ✅ 构建了 COCO 数据集的 *多视图分析系统*
    - ✅ 实现了 *空间-语义-姿态* 三维度探索
    - ✅ 设计了 *Cross-Filtering* 联动交互
    - ✅ 揭示了视觉数据中的 *模式与洞察*
    
    #v(1em)
    
    === 技术亮点
    
    - 沉浸式门户的 *滚动叙事* 设计
    - 统一的 *DESIGN Token* 系统
    - 完整的 *80 类别* 支持
  ],
  [
    === 未来改进
    
    - 🔮 增加时序分析（视频数据）
    - 🔮 支持更多数据集（LVIS、Objects365）
    - 🔮 添加模型预测结果对比
    - 🔮 优化大规模数据渲染性能
    
    #v(1em)
    
    === 致谢
    
    感谢 COCO 数据集团队的开源贡献
    
    感谢课程老师的指导
  ]
)

// ============================================================================
// 结束页
// ============================================================================
== 感谢聆听

#align(center)[
  #text(size: 2em, weight: "bold")[🌌 COCO-Verse]
  
  #v(0.5em)
  
  #text(size: 1.2em)[Decoding Common Objects in Context]
  
  #v(1em)
  
  #text(fill: gray)[
    代码仓库：#link("https://github.com/xzxxntxdy/Data-Visualization-Coursework")[https://github.com/xzxxntxdy/Data-Visualization-Coursework]
    
    演示地址：#link("http://localhost:8080")[localhost:8080]
  ]
  
  #v(2em)
  
  Q & A
]